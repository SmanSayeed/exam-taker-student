import { Link } from "react-router-dom";

const Logo = () => {
    return (
        <div>
            <Link to="/" className="text-3xl font-semibold">
                OES
            </Link>
        </div>
    )
}

export default Logo;